"use client";

import { NextScript } from "next/document";
import { useEffect } from "react";

export const GoogleAnalytics = () => {
	useEffect(() => {
		if (window !== undefined) {
		}
	}, []);
	return <NextScript />;
};
